using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace PerspirationPlugin
{
    /***************************** DELEGATES ***********************************************/
    //Define the IntDelegate
    public delegate void IntDelegate(int value);

    

    /***************************** INTERFACES ***********************************************/

    /*
    
    // Define the Interface
    public interface IBitmapData : IPinData
    {
        // Properties
        Bitmap Value { get; }
    }

    // Define the interface implementation 
    public class BitmapData : IBitmapData, IPinData
    {
        protected Bitmap value;

        public BitmapData(Bitmap setValue)
        {
            value = setValue;
        }

        public Bitmap Value
        {
            get
            {
                return value;
            }
        }
     */
}
